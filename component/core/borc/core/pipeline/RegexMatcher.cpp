
#include "RegexMatcher.hpp"

namespace borc {
    RegexMatcher::RegexMatcher(const std::string &name, const std::string &regexPattern) {
    }

    RegexMatcher::~RegexMatcher() {
    }

    bool RegexMatcher::match(const std::string &fileName) {
        return true;
    }
}
